<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:Gestures.Views"
             x:Class="Gestures.Views.MainPage"
             Title="Markup Extensions"
             Padding="10">
    <TableView Intent="Menu">
        <TableRoot>
            <TableSection>
                <TextCell Text="Tap Gesture Demo"
                          Detail="Tap inside an image or frame"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type views:TapCommandPage}" />

                <TextCell Text="Swipe Gesture Demo"
                          Detail="Swipe up, down, left or right"
                          Command="{Binding NavigateCommand}"
                          CommandParameter="{x:Type views:SwipeCommandPage}" />
            </TableSection>
        </TableRoot>
    </TableView>
</ContentPage>    